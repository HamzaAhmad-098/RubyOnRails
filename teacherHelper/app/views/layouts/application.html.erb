<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Teacher AI Assistant" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Optional PWA support %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# JavaScript %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%= yield :head %>
  </head>

  <body class="bg-slate-50 text-gray-800 min-h-screen font-sans">
    <!-- NAVBAR -->
    <nav class="bg-indigo-700 text-white px-6 py-4 shadow-md">
<div class="container mx-auto flex justify-between items-center">
  <h1 class="text-xl font-bold">📘 Teacher AI Assistant</h1>

  <div class="flex items-center gap-4">
    <%= link_to "🏠 Home", root_path, class: "hover:underline" %>

    <% if user_signed_in? %>
      <%= button_to "🚪 Logout", destroy_user_session_path, method: :delete %>

    <% else %>
      <%= link_to "🔐 Login", new_user_session_path, class: "hover:underline" %>
    <% end %>
  </div>
</div>

    </nav>

    <!-- FLASH MESSAGES -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 mx-auto my-4 rounded max-w-3xl">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 mx-auto my-4 rounded max-w-3xl">
        <%= alert %>
      </div>
    <% end %>
     
    <!-- MAIN CONTENT -->
    <main class="container mx-auto px-4 py-6">
      <%= yield %>
    </main>

    <!-- FOOTER -->
    <footer class="bg-slate-800 text-white text-center py-4 mt-10">
      <p class="text-sm">&copy; 2025 Teacher AI Assistant. Powered by Ruby on Rails + OpenAI</p>
    </footer>
  </body>
</html>
